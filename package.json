{
  "name": "hackathon-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix TrashTrail",
    "dev:https": "npm run dev:https --prefix TrashTrail",
    "turbo": "npm run turbo --prefix TrashTrail"
  }
}